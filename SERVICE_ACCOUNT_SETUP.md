# 🔑 Service Account Key Setup - ทำตามนี้เลย!

## ✅ สิ่งที่ทำเสร็จแล้ว:
- ✅ อัพเดท `firebaseConfig.js` ให้รองรับ Service Account Key

## 📋 สิ่งที่คุณต้องทำตอนนี้:

### ขั้นตอนที่ 1: ดาวน์โหลด Service Account Key
1. ใน Firebase Console (ที่คุณเปิดอยู่)
2. คลิกปุ่ม **"Generate new private key"** (สีน้ำเงิน)
3. คลิก **"Generate key"** อีกครั้งในป๊อปอัพเพื่อยืนยัน
4. ไฟล์ JSON จะถูกดาวน์โหลด (ชื่อจะคล้าย `kohtao-nightboat-xxxxx.json`)

### ขั้นตอนที่ 2: ย้ายไฟล์
1. ไปที่โฟลเดอร์ **Downloads**
2. หาไฟล์ที่เพิ่งดาวน์โหลด (ชื่อขึ้นต้นด้วย `kohtao-nightboat-`)
3. **คัดลอกหรือย้าย** ไฟล์นี้ไปยัง:
   ```
   c:\Users\NB-Watcharapong\Desktop\kohtao-nightboat\kohtao-nightboat\server\
   ```
4. **เปลี่ยนชื่อไฟล์** เป็น: `serviceAccountKey.json`

### ขั้นตอนที่ 3: อัพเดทไฟล์ .env
เปิดไฟล์ `server/.env` และแก้ไขให้เป็น:

```env
GOOGLE_APPLICATION_CREDENTIALS=./serviceAccountKey.json
SECRET_KEY=supersecretkey
PORT=3001
```

**หมายเหตุ:** ลบหรือ comment บรรทัด `FIREBASE_PROJECT_ID=...` ออก

### ขั้นตอนที่ 4: ทดสอบ
```bash
npm run seed
```

**ผลลัพธ์ที่ควรเห็น:**
```
✅ Firebase initialized with service account key
✅ Firestore connected successfully
🌱 Starting Firestore database seed...
✅ Created admin user: admin
✅ Created staff user: staff
✅ Created sample ticket: xxxxx
✅ Created sample parcel: xxxxx
🎉 Firestore database seeded successfully!
```

### ขั้นตอนที่ 5: เริ่มต้น Server
```bash
npm run dev
```

---

## 📁 โครงสร้างไฟล์ที่ถูกต้อง:

```
server/
├── .env                          ← มี GOOGLE_APPLICATION_CREDENTIALS
├── serviceAccountKey.json        ← ไฟล์ที่ดาวน์โหลดมา (เปลี่ยนชื่อแล้ว)
├── firebaseConfig.js             ← อัพเดทแล้ว
├── index.js
├── seed.js
└── package.json
```

---

## ⚠️ สำคัญมาก!

1. **ไฟล์ `serviceAccountKey.json` มีข้อมูลสำคัญ** - อย่า commit ขึ้น Git!
2. ไฟล์นี้ถูก gitignore ไว้แล้ว ตรวจสอบได้ที่ `.gitignore`
3. **อย่าแชร์ไฟล์นี้ให้ใครเห็น** - มันเหมือนรหัสผ่านของ Firebase

---

## 🆘 หากมีปัญหา:

### Error: "Cannot find module './serviceAccountKey.json'"
**สาเหตุ:** ไฟล์ไม่อยู่ในตำแหน่งที่ถูกต้องหรือชื่อไฟล์ไม่ตรง

**วิธีแก้:**
1. ตรวจสอบว่าไฟล์อยู่ใน `server/serviceAccountKey.json`
2. ตรวจสอบว่าชื่อไฟล์สะกดถูกต้อง (ตัวพิมพ์เล็ก-ใหญ่ต้องตรงกัน)

### Error: "Unexpected token" หรือ "JSON parse error"
**สาเหตุ:** ไฟล์ JSON เสียหาย

**วิธีแก้:**
1. ลบไฟล์ `serviceAccountKey.json`
2. ดาวน์โหลดใหม่จาก Firebase Console
3. ย้ายและเปลี่ยนชื่อใหม่

---

**พร้อมแล้ว! ทำตามขั้นตอนที่ 1-5 เลยครับ** 🚀
