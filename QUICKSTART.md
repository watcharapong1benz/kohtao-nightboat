# 🚀 Quick Start Guide - Ko Tao Night Boat

## การเริ่มต้นใช้งานครั้งแรก (Windows)

### วิธีที่ 1: ใช้ Quick Start Script (แนะนำ)

1. **ดับเบิ้ลคลิก** `start-dev.bat`
2. ถ้าเป็นครั้งแรก จะถามว่าต้องการสร้างข้อมูลตัวอย่างหรือไม่ → กด `y`
3. รอให้ server และ client เริ่มทำงาน
4. เปิดเบราว์เซอร์ไปที่ http://localhost:5173

**ข้อมูล Login:**
- Admin: `admin` / `admin123`
- Staff: `staff` / `staff123`

### วิธีที่ 2: รันแบบ Manual

**Terminal 1 - Server:**
```bash
cd server
npm install
npm run seed    # ครั้งแรกเท่านั้น
npm start
```

**Terminal 2 - Client:**
```bash
cd client
npm install
npm run dev
```

## การใช้งานระบบ

### 1. Login
- เปิด http://localhost:5173
- ใส่ username และ password
- กด Login

### 2. ขายตั๋วเรือ
1. ไปที่เมนู **"ขายตั๋ว"**
2. เลือกประเภทเรือ (50 ที่ / 30 ที่)
3. เลือกเส้นทาง (สุราษฎร์ธานี ↔ เกาะเต่า)
4. เลือกวันที่เดินทาง
5. **คลิกเลือกที่นั่ง** บนแผนที่
6. กรอกข้อมูลผู้โดยสาร
7. กด **"ยืนยันการขาย"**

### 3. รับฝากพัสดุ
1. ไปที่เมนู **"รับฝากพัสดุ"**
2. กรอกข้อมูลผู้ส่ง
3. กรอกข้อมูลผู้รับ
4. ใส่น้ำหนัก → ราคาจะคำนวณอัตโนมัติ
5. เลือกสถานะการชำระเงิน
6. กด **"บันทึกรับฝาก"**

### 4. ดูรายการตั๋ว/พัสดุ
1. ไปที่เมนู **"รายการตั๋ว"** หรือ **"รายการพัสดุ"**
2. เลือกวันที่ที่ต้องการดู
3. สามารถ:
   - 🖨️ **Print** - พิมพ์ใบเสร็จ
   - ✏️ **Edit** - แก้ไขข้อมูล
   - 🗑️ **Delete** - ลบรายการ

### 5. Dashboard
- แสดงสรุปยอดขาย/พัสดุของวันนี้
- รายได้รวม
- พัสดุที่รอจัดส่ง

## การ Deploy บน Server จริง

### สำหรับ VPS/Cloud Server (Linux)

1. **Upload โปรเจกต์ไปยัง server**
```bash
git clone <your-repo>
cd kohtao-nightboat
```

2. **รัน deployment script**
```bash
chmod +x deploy.sh
./deploy.sh
```

3. **ตั้งค่า Nginx**
```bash
sudo cp nginx.conf /etc/nginx/sites-available/kohtao
sudo ln -s /etc/nginx/sites-available/kohtao /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

4. **ตั้งค่า SSL (Let's Encrypt)**
```bash
sudo apt install certbot python3-certbot-nginx
sudo certbot --nginx -d your-domain.com
```

### สำหรับ Windows Server

1. **รัน deployment script**
```
deploy.bat
```

2. **ติดตั้ง IIS หรือ Web Server อื่นๆ**
3. **Point document root ไปที่** `client/dist`
4. **ตั้งค่า reverse proxy** สำหรับ `/api` ไปยัง `http://localhost:3001`

## การอัพเดทระบบ

```bash
# Pull code ใหม่
git pull

# อัพเดท server
cd server
npm install
npx prisma generate
pm2 restart kohtao-server

# อัพเดท client
cd ../client
npm install
npm run build
```

## Troubleshooting

### ❌ Server ไม่ทำงาน
- ตรวจสอบว่า port 3001 ไม่ถูกใช้งาน
- ตรวจสอบ `.env` file ใน server/
- ดู logs: `pm2 logs kohtao-server`

### ❌ Client ไม่เชื่อมต่อ API
- ตรวจสอบ `client/.env` ว่ามี `VITE_API_URL` ถูกต้อง
- ตรวจสอบว่า server ทำงานอยู่

### ❌ Database error
```bash
cd server
npx prisma db push
npx prisma generate
```

### ❌ ลืม password admin
```bash
cd server
npm run seed    # จะ reset admin/staff users
```

## การ Backup ข้อมูล

### Backup Database
```bash
# SQLite
cp server/dev.db server/backup-$(date +%Y%m%d).db
```

### Restore Database
```bash
cp server/backup-20231215.db server/dev.db
pm2 restart kohtao-server
```

## ติดต่อและสนับสนุน

หากมีปัญหาหรือข้อสงสัย:
1. ตรวจสอบ README.md
2. ดู logs ของ server และ client
3. ติดต่อทีมพัฒนา

---

**สร้างโดย:** Ko Tao Night Boat Team  
**เวอร์ชัน:** 1.0.0  
**อัพเดทล่าสุด:** 2024
