# Production environment - empty string means no prefix
# Client will call /dashboard, /tickets, etc.
# Firebase Hosting will rewrite /api/** to Cloud Functions
# So we need to call with /api prefix from client
VITE_API_URL=
