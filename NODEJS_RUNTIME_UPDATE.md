# ✅ แก้ไข Node.js Runtime แล้ว!

## 🔧 การเปลี่ยนแปลง:

ได้อัพเดทไฟล์ต่อไปนี้แล้ว:

1. ✅ `functions/package.json` - เปลี่ยนจาก Node.js 18 → **Node.js 20**
2. ✅ `firebase.json` - เปลี่ยน runtime เป็น **nodejs20**
3. ✅ `deploy-firebase.bat` - เพิ่มข้อความแจ้งเตือน

---

## 🚀 พร้อม Deploy แล้ว!

ตอนนี้คุณสามารถ deploy ได้เลย:

### วิธีที่ 1: ใช้ Script
```bash
.\deploy-firebase.bat
```

### วิธีที่ 2: Manual
```bash
firebase deploy
```

---

## 📝 หมายเหตุ:

- Node.js 18 ถูก decommission เมื่อ 30 ตุลาคม 2025
- ตอนนี้ใช้ **Node.js 20** แทน (รองรับถึง 30 เมษายน 2026)
- ไม่ต้องติดตั้ง Node.js 20 บนเครื่องของคุณ - Firebase จะใช้ Node.js 20 บน Cloud Functions อัตโนมัติ

---

## 🎯 ขั้นตอนถัดไป:

1. รัน `firebase deploy`
2. รอให้ deploy เสร็จ (3-5 นาที)
3. เปิดเว็บไซต์ที่ `https://kohtao-nightboat.web.app`

**พร้อมแล้ว! Deploy ได้เลยครับ** 🚀
